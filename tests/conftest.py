"""
Shared pytest configuration.

The pytest-asyncio plugin is configured via pytest.ini (asyncio_mode = auto),
so all async test functions are automatically treated as asyncio coroutines.
"""
import pytest


# Additional fixtures or configuration can be added here as needed.
